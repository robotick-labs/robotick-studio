{
  "name": "robotick-cli",
  "private": true,
  "version": "0.0.0",
  "description": "Studio-owned CLI for Robotick workspaces",
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test test"
  }
}
